package com.example.a3200final

import android.graphics.Bitmap
import android.media.Image
import android.widget.ImageView
import androidx.lifecycle.MutableLiveData
import androidx.lifecycle.ViewModel
import kotlinx.coroutines.flow.MutableStateFlow
import kotlinx.coroutines.flow.StateFlow

class ImageViewModel(topImageView: ImageView, bottomImageView: ImageView): ViewModel() {
    /*
    fun updateImages(bmp: Bitmap) {
        bottomImageView .setImageBitmap(topImageView as Bitmap)
        topImageView.setImageBitmap(bmp)
    }


     */
}